# 分流规则维护规范

本文件中的“必须”“禁止”“仅”均为执行约束。AI 必须先识别任务类型，再执行对应流程；完成检查前不得宣称任务完成。
目标：基于 fork 的 [ios_rule_script](https://github.com/blackmatrix7/ios_rule_script)，在 `Anm/` 内维护多软件个人分流规则，使分流意图一致、格式适配，并能仅凭 README 在上游更新后重新整理，不依赖聊天记录。

## 1. 写入边界与软件范围

- **允许写入**：所有项目内容变更仅限 `Anm/`。用户明确要求维护本文件时，才允许修改根目录 `AGENTS.md`；该授权不得扩展到其他上游内容。
- **上游只读**：`Anm/` 外的项目文件和目录均视为原始 fork 内容，仅可读取、检索或作为来源。禁止修改、新增、删除、移动、重命名，以及通过脚本、格式化或生成工具间接改写。
- **保留现场**：必须保留用户已有修改、暂存内容和删除操作，禁止擅自还原、覆盖或混入任务。不得主动拉取、合并或重置上游；上游同步由用户另行安排。
- **交付范围**：默认只完成本地整理与验证；提交、推送、发布必须已有用户授权。新增配置引用不代表远程文件已发布。
- **目录约定**：上游为 `rule/<软件>/`；自定义规则为 `Anm/<软件>/rule/`；配置位于 `Anm/<软件>/`；记录统一为 `Anm/<软件>/README.md`，禁止另建 `READM.md`。必须沿用已有分类和命名。
- **活动软件**：目前为 Surge、Loon；每次操作前必须检查实际已接入软件。新软件必须建立对应目录与 README，明确配置文件和可修改模块，并纳入同步范围。`Anm/discarded/` 是归档，禁止纳入活动软件或自动恢复其中规则。

## 2. 任务路由与规则更新

- **规则修改任务**：新增、更新、合并、去重、删除、迁移规则或调整规则配置时，必须检查并同步所有活动软件的对应规则、必要引用和 README，不能只处理请求中点名的软件。
- **只读对比任务**：“xxx 对比 yyy”必须执行第 3 节，只输出结果，不修改规则、配置或 README；只有另行要求合并、删除或替换时才进入修改流程。禁止把全软件修改同步要求套用到默认 Surge 对比。
- **“更新自定义规则”**：必须依次执行以下步骤，不得扩展到 README 未记录的规则、清空目录或删除未记录文件：
  1. 读取本规范和所有活动软件的 README，确认各模块当前有效的来源、目标、操作顺序、转换、合并、手动补充及排除项。
  2. 检查工作区既有修改，以用户已更新的本地 fork 和当前来源链接为基础读取内容，不改写来源。
  3. 按记录重新整理：整份导入重新读取当前来源；局部合并、删除和替换按记录应用。禁止在旧结果上盲目追加；必须保留仍有效的其他来源和用户补充，避免重新引入上游已删除规则。
     对比筛选类操作必须先整理其依赖的对比目标，再按第 3.2 节对当前来源重新判断覆盖，重建该操作所属规则组，并按第 6 节的数量门槛更新 README 操作条目及所需代码块；禁止把上次结果当作固定补充，或擅自将合并目标加入对比目标造成自覆盖。固定选取与持续排除则按记录的规则或条件执行。
  4. 核对新增、删除、去重、转换和语义变化；来源消失、无法读取或转换失效时，保留现有有效内容，报告具体问题及未完成项，禁止编造规则。
  5. 同步各软件规则、必要引用及有变化的 README 条目，执行第 7 节检查，再简要报告改动文件、规则变化、同步结果及待处理项。
- **跨软件适配**：必须以分流意图和有效覆盖范围为准，优先使用上游对应软件来源；缺少时可转换指定来源，但必须记录方法。禁止为凑数量复制不兼容语法，或改变域名匹配、IP 网段、DNS 行为、策略和顺序。
- **不支持的规则**：必须先检查等价转换；不能转换时保留已有有效行为，在 README 操作条目末尾简注差异，并在交付说明中解释影响和原因。禁止静默省略或将部分同步声称为全部完成。
- **无需修改的软件**：检查后保留原规则、更新时间和 README，不追加检查记录；仅在交付说明中说明结论及原因。

## 3. 只读对比：定位、覆盖与输出

### 3.1 定位输入

- `xxx` 是任意非自定义规则文件或用户提供的外部链接，可位于上游 `rule/`、仓库其他目录或仓库外；来源始终只读。
- `yyy` 可以是一个或多个目标，每个都必须是 `Anm/<软件>/rule/` 下的自定义文件。禁止将双方都定位到自定义目录，也不要求该目录存在 `xxx`。
- `yyy` 只给文件名且未指定软件时，必须只查找 `Anm/Surge/rule/` 并对比 Surge；显式指定软件或自定义路径时使用指定目标，禁止自动扩展到其他软件。
- 来源优先使用明确路径、链接或上下文；只有文件名且存在多个软件版本时，优先使用目标软件对应来源。禁止因 `rule/` 中不存在便断言来源缺失，也不得扫描无关目录。
- 多个候选无法由上下文确定，或仅凭文件名无法定位来源时，才询问具体路径。来源无法读取或目标不存在时必须如实说明，不得擅自替换。
- 例：“AliPay.list 对比 China_All.list 和 ChinaMedia.list”默认读取 `rule/Surge/AliPay/AliPay.list`，与 `Anm/Surge/rule/Inner/China/China_All.list`、`Anm/Surge/rule/Inner/ChinaMedia/ChinaMedia.list` 联合对比；只有一个目标时规则相同。

### 3.2 强制按代理规则语义判断

- 必须按 Surge、Loon、Clash、Stash 等对应软件的实际分流语义解析来源和目标。禁止以文本、行、文件 `diff` 或字符串集合差集作为最终覆盖结果。
- 必须逐条判断源规则的全部匹配范围是否被全部目标的一条或多条有效规则完整包含；完整包含计覆盖，部分重叠不计完整覆盖，每条源规则最多计一次。
- 多目标必须联合判断：任一目标独立覆盖或多个目标共同完整覆盖都算覆盖。禁止拼接单文件缺失项，也禁止仅合并各自已覆盖结果而漏掉跨文件共同覆盖。
- 无策略规则集比较匹配范围；含策略、组合条件或影响匹配的选项时，必须同时确认条件及分流行为兼容，禁止仅凭域名或 IP 相同认定覆盖。
- 域名必须按标签边界判断包含：目标 `DOMAIN-SUFFIX,acfun.cn` 覆盖源 `DOMAIN-SUFFIX,b.acfun.cn` 和 `DOMAIN,b.acfun.cn`；反向不成立，也不覆盖 `notacfun.cn`。关键词、通配符、正则必须使用对应软件语义。
- IP 必须解析 IPv4/IPv6 和 CIDR，检查单个网段或多个目标网段并集的完整包含；禁止把字符串相同或网段相交作为唯一依据。DNS、`no-resolve` 等选项必须按软件语义检查其对匹配范围的影响。
- 进程、User-Agent、端口、协议、ASN、GEOIP、逻辑组合、规则集引用及跨软件等价语法、域名表格式，必须解析为可比较含义。语法不明时查阅对应软件官方文档，按需要读取引用或数据库；禁止猜测覆盖。
- 不能完成判断时，必须在表格相关单元格标注“待确认”，禁止伪装成确定的未覆盖结果。

### 3.3 统计与最终输出

- 统计前必须剔除注释、空行和完全重复项；目标数是所有目标合并后去除完全重复项的总数。禁止因包含关系而从规则总数中删除规则。
- 覆盖数是被目标联合完整覆盖的源规则数；全部判定完成时，未覆盖数＝源规则数－覆盖数。部分覆盖必须按原源规则计入未覆盖，禁止擅自拆分生成新规则。
- 完成后必须直接在回复中渲染 Markdown 表格，仅一行联合统计，固定四列：`源文件规则数`、`目标文件规则数`、`源文件覆盖规则数`、`未覆盖规则数`。禁止生成 CSV/Excel 等文件、附加解释段落或单文件缺失统计。
- 最终未覆盖 **1–49 条**：表格下方必须且仅可追加一个 `text` 代码块，按来源顺序去重列出全部最终未覆盖的原规则。
- 最终未覆盖 **0 条或 ≥50 条**：仅输出表格，禁止代码块、省略列表或中间差异。任何情况下均禁止输出目标独有规则，或某个目标缺失但已被其他目标覆盖的规则。

## 4. 规则文件的整理要求

- 必须清除来源注释、标题、统计头部，仅保留有效规则；清理行内注释不得截断规则值。来源不明或不可读时保留现有有效内容并报告未完成项，禁止编造。
- 每个来源或用途独立的规则组前必须依次保留且仅保留下列三项注释，不得保留源 `NAME`、`AUTHOR`、`REPO`、`TOTAL` 等头部：

  ```text
  # SOURCE: rule/Surge/China/China_All.list
  # UPDATE: yyyy-MM-dd HH:mm:ss
  # REMARK: 国内网站及相关服务规则，用于直连分流。
  ```

- `SOURCE` 必须是可定位的仓库相对路径或完整 URL；多个来源分别成组，禁止模糊来源。手动补充必须标为“用户自定义”，并指向 README 中保存具体规则的条目。
- `UPDATE` 必须使用实际整理时间，时区 `Asia/Shanghai`，格式 `yyyy-MM-dd HH:mm:ss`；同批变更使用同一时间，只检查或未变更的组不得刷新。
- `REMARK` 必须用中文说明用途，转换或语义差异须简述。软件不支持 `#` 时使用合法注释语法；完全不支持注释时，在旁边的 `<规则文件名>.meta.md` 保存三项字段，并在 README 条目简注差异。
- 合并默认仅移除语义完全相同的重复项，保留顺序和来源归属；多来源重复规则保留首次出现项，并在 README 合并条目保留各来源关系。
- **语义覆盖不授权删规则**：禁止仅因域名后缀或网段包含而删除规则，也不得擅自改变策略、DNS 选项或优先级；确需调整必须记录依据及影响。

## 5. 配置修改要求

| 软件 | 配置文件 | 默认可修改模块 |
| --- | --- | --- |
| Surge | `Anm/Surge/Surge.conf` | `[Rule]` |
| Loon | `Anm/Loon/Loon.lcf` | `[Remote Rule]` |

- 未经用户明确授权，禁止修改表中其他模块。新软件必须由用户或适用规范明确修改范围；不明确时先整理规则，再说明配置待确认项。
- 规则链接前仅写中文用途说明，如 `# > 国内网站及相关服务直连`；禁止添加 `SOURCE`、`UPDATE`、`REMARK` 元信息。
- 必须核对策略、路径、启用状态和顺序，避免重复引用；迁移或删除规则必须同步引用，新引用不得被前置宽泛规则或最终规则遮蔽。
- 必须沿用已确认订阅约定，不得根据本地路径猜测发布地址。订阅仓库不同于当前 fork 时，仅在交付说明中说明映射及发布前提，不写入 README。

## 6. README 必须保持简洁

- 每个软件仅按规则模块分组，使用 `## 模块名` 和无序操作列表；每条默认一句话，必须写清来源、动作和目标。文件使用仓库相对路径，链接使用完整 URL。
- 整份文件或链接导入只写“将 `xxx` 内容复制到 `yyy`。”或“将 `xxx` 内容合并到 `yyy`。”，禁止展开代码块；剔除源注释、格式转换及常规去重不改变整份导入性质。
- 格式转换使用“将 `xxx` 按 `必要转换方法` 转换后写入 `yyy`。”；必要的软件差异只可用短句或括号说明，不另写分析章节。
- 条目必须保留决定规则如何选取的条件及操作顺序：对比筛选必须写清来源、全部对比目标、选取未覆盖还是已覆盖规则及写入目标，禁止简化成“将以下规则合并”。各软件记录实际使用的对应路径；固定选取、动态对比和持续排除不得混写。
- 对比筛选类操作按每条操作实际新增或删除的去重规则数决定是否记录代码块（不是源文件或对比目标的规则总数）：**1–50 条**须记录完整代码块；**超过 50 条**仅保留一条完整操作说明，不写代码块，并移除该操作已有的代码块；来源、全部对比目标、筛选条件、操作顺序、转换方法、写入目标及必要软件差异仍须保留。此门槛仅适用于 README，不改变第 3.3 节只读对比回复的输出规则。
- 除上述超过 50 条的对比筛选操作外，局部合并、添加、删除或替换必须在操作条目下留一个空行，紧接代码块，完整记录本地实际变更：新增/合并列实际新增规则，删除列实际删除规则，均用 `text`；替换或同时增删用 `diff`，以 `-` 表示旧规则、`+` 表示新规则。
- 代码块禁止包含整个来源、未变规则、未写入重复项或省略号；条目与代码块间禁止插入说明。没有实际变更不得编造内容或追加记录。
- 同步各软件当前有效条目；相同操作不得重复追加，来源或方式改变时更新原条目，无变化不改。禁止重复记录清理注释、三项注释和常规去重等通用要求。
- 禁止添加维护范围、重建步骤章节、操作历史、日期、提交号、统计、验证报告、配置引用、发布说明或重复的三项注释。

### 6.1 操作条目模板

以下模板仅规定已授权操作的记录方式，不授予删除、替换、迁移或扩大处理范围的权限。占位项必须换成实际路径、完整 URL 或明确条件；单个对比目标直接写该目标，多个目标必须全部列出并联合判断。代码块遵循本节的数量门槛及内容要求，保存当前有效操作的实际规则，不充当历史日志；仅修正文案且规则未变时保留仍符合数量门槛的原代码块。

| 操作 | 条目模板 | 紧随的代码块 |
| --- | --- | --- |
| 整份复制或合并 | 将 `来源` 内容复制到／合并到 `目标`。 | 无 |
| 整份转换导入 | 将 `来源` 按 `必要转换方法` 转换后写入 `目标`。 | 无 |
| 固定选取部分规则 | 将 `来源` 中的以下规则合并到 `目标`。 | `text`：实际新增规则 |
| 对比后补充未覆盖规则 | 将 `来源` 与 `对比目标一` 和 `对比目标二` 联合对比，将未覆盖的规则合并到 `合并目标`。 | 1–50 条用 `text` 记录实际新增的未覆盖原规则；超过 50 条无代码块 |
| 对比后选取已覆盖规则 | 将 `来源` 与 `对比目标一` 和 `对比目标二` 联合对比，将已覆盖的规则合并到 `合并目标`。 | 1–50 条用 `text` 记录实际新增的已覆盖原规则；超过 50 条无代码块 |
| 条件筛选或局部转换 | 将 `来源` 中符合 `明确条件` 的规则按 `必要转换方法` 转换后合并到 `目标`（无需转换时省略转换短语）。 | `text`：实际新增规则 |
| 用户自定义补充 | 将用户自定义的以下规则添加到 `目标`。 | `text`：实际新增规则 |
| 固定删除或持续排除 | 从 `目标` 中删除以下规则／将 `来源` 内容导入 `目标` 后排除以下规则。 | `text`：实际删除或排除的规则 |
| 对比后删除已覆盖规则 | 将 `来源` 与 `对比目标一` 和 `对比目标二` 联合对比，从 `待修改目标` 中删除已被覆盖的规则。 | 1–50 条用 `text` 记录实际删除规则；超过 50 条无代码块；条目须简注已授权的删除依据及影响 |
| 局部替换 | 将 `目标` 中的以下规则按 `替换来源或用户指定依据` 替换。 | `diff`：旧规则与新规则 |
| 整份更换来源 | 将 `新来源` 内容复制到 `目标`（更新原导入条目，其他仍有效来源及用户补充按原条目保留）。 | 无；涉及额外局部增删时另用对应模板 |
| 规则迁移 | 将 `原自定义文件` 中的以下规则迁移到 `新自定义文件`。 | `text`：实际迁移规则；更新原来源条目的目标路径并保留来源关系 |

- 同一操作同时包含筛选、转换、排除等步骤时，按实际执行顺序组合模板，保留全部必要条件；多个来源分别记录，禁止用“相关规则”等模糊描述代替来源。
- 常规完全重复项去重沿用本节规定，不另写条目；按语义覆盖删除必须使用明确的删除模板并已有授权。配置引用调整仍只修改配置，不写入 README。
- 已有动态筛选条目在更新后没有有效规则时，保留筛选条件并移除失效的规则代码块；不得伪造规则或追加“本次无变化”等检查记录。尚无条目且没有实际变更时，不新增条目。

## 7. 完成前必须核查

- 写入仅在授权范围内，未改写上游或干扰用户既有修改；对比任务没有任何文件写入。
- 修改任务已检查全部活动软件；规则、必要引用和 README 一致，软件差异及未完成项明确，不得仅凭数量相同宣称语义一致。
- 规则没有遗漏或意外增项，去重符合记录，三项注释合法完整、无残留源注释；配置模块、路径、策略和顺序合规。
- README 仅含模块、简洁条目及局部变更所需代码块；对比结果符合联合语义覆盖和第 3.3 节输出要求。
- 必须检查差异和空白格式；有可用工具时校验目标软件语法。未运行客户端验证、未发布或未确认生效时必须如实说明，禁止声称已生效。
