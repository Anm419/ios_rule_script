# 项目目标

基于 fork 的 [blackmatrix7/ios_rule_script](https://github.com/blackmatrix7/ios_rule_script)，在 `Anm/` 内维护个人分流规则。目前支持 Surge 和 Loon，后续可接入其他分流软件。各软件应表达一致的分流意图，并适配各自支持的规则格式。维护记录必须足以在上游更新后重新整理自定义规则，不能依赖聊天记录。

# 全局强制约束

1. 所有项目内容变更必须限定在 `Anm/` 内。目录外的所有文件和目录均视为原始 fork 内容，只允许读取、检索和作为规则来源；禁止修改、新增、删除、移动、重命名，或通过脚本、格式化、生成工具等间接改写。
2. 用户明确要求维护本文件时，允许仅修改根目录 `AGENTS.md`；这不构成修改其他上游内容的授权。
3. 源规则必须整理后写入 `Anm/`，不得在源文件上直接修改。不得主动执行会改写上游文件的拉取、合并或重置操作；上游同步由用户另行安排。
4. 保留用户已有的修改、暂存内容和删除操作，不得擅自还原、覆盖或混入本次任务。
5. 默认完成本地整理与验证；提交、推送、发布须有用户授权。新增配置引用不代表远程规则已经发布。

# 目录与软件范围

- 上游来源：`rule/<软件>/`，只读；源链接也可作为来源。
- 自定义规则：`Anm/<软件>/rule/`，沿用已有分类和命名，例如 `Inner/China/China_All.list`。
- 软件操作记录：`Anm/<软件>/README.md`，统一使用 `README.md`，不另建 `READM.md`。
- 软件配置：位于对应的 `Anm/<软件>/` 下。
- 当前已接入软件为 Surge、Loon。后续增加软件时，必须建立对应目录和 README，明确配置文件及允许修改的规则模块，并纳入每次同步范围；README 仅按下方模板记录规则操作。
- 每次操作前检查 `Anm/` 下实际已接入的软件，不能只处理请求中点名的软件。`Anm/discarded/` 是历史归档，不属于活动软件，不得从中自动恢复旧规则。

# 常用指令的执行约定

1. **“更新自定义规则”**：读取所有已接入软件的 `Anm/<软件>/README.md`，按各模块记录的来源、目标和操作顺序，执行下方“上游更新后的维护流程”，更新各软件自定义目录中的对应规则。整份导入重新读取当前来源，局部合并、删除和替换按记录应用；同步必要的配置引用及有变化的 README 条目。不自行扩大到未记录的规则，也不清空目录或删除未记录的文件。
2. **“xxx文件对比yyy文件”或“xxx链接对比yyy文件”**：

   - **来源 `xxx`**：任意非自定义规则文件或用户提供的外部规则链接；不限于上游 `rule/`，也可以位于仓库其他目录或仓库外。来源只读，位置不影响现有修改范围约束。
   - **目标 `yyy`**：必须是 `Anm/<软件>/rule/` 下的自定义规则文件。不得将双方都定位到自定义目录，也不要求自定义目录中存在 `xxx`。
   - **默认软件**：`yyy` 只给文件名、未指定软件时，始终在 `Anm/Surge/rule/` 下查找，仅对比 Surge 目标，不自动扩展到其他软件；显式指定软件或自定义路径时，使用指定目标。
   - **来源定位**：优先使用用户给出的路径、链接或明确的上下文。仅给来源文件名时查找非自定义来源；有多个软件版本时，优先使用与目标软件对应的来源。不因 `rule/` 中不存在就认定来源缺失，也不擅自扫描无关目录。
   - **定位异常**：存在多个候选且无法由上下文确定，或仅凭文件名无法定位来源时，才明确具体路径。来源无法读取或目标不存在时如实说明，不擅自替换。
   - **强制语义对比**：这是 Surge、Loon、Clash、Stash 等代理软件的网络分流规则覆盖对比，不是文本、行或文件差异对比。必须识别来源格式和目标软件的实际规则语义；文本不相同不代表未覆盖，不得直接把 `diff` 或字符串集合差集作为最终结果。
   - **覆盖判定**：逐条检查源规则匹配的全部流量范围，是否被目标文件的一条或多条有效规则完整包含。完整包含计为覆盖，仅部分重叠不计为完整覆盖；每条源规则最多计一次。没有策略的规则集比较匹配范围；带策略、组合条件或影响匹配的选项时，还须检查条件和分流行为是否兼容，不得仅凭域名或 IP 相同判定覆盖。
   - **域名覆盖**：按域名标签边界判断 `DOMAIN`、`DOMAIN-SUFFIX` 等规则的包含关系。例如目标 `DOMAIN-SUFFIX,acfun.cn` 已覆盖源 `DOMAIN-SUFFIX,b.acfun.cn` 及 `DOMAIN,b.acfun.cn`；反向不成立，`notacfun.cn` 也不能因字符串结尾相似被判定覆盖。关键词、通配符和正则须按对应软件的实际匹配语义判断，不得套用普通后缀比较。
   - **IP 覆盖**：解析 IPv4/IPv6 地址和 CIDR 网段，检查目标网段或多个目标网段的并集能否完整覆盖源网段；不能只比较网段字符串，也不能将相交误判为完整覆盖。DNS 解析和 `no-resolve` 等选项须按目标软件判断其对匹配范围的影响。
   - **其他类型与格式差异**：进程、User-Agent、端口、协议、ASN、GEOIP、逻辑组合及规则集引用等，均按对应软件语义处理。不同软件的等价语法或域名表格式先解析为可比较的含义；无法确认的语法查阅对应软件官方文档，引用或数据库按需要读取。不能证明包含关系时，不得猜测覆盖；无法完成判断须在表格对应单元格标注“待确认”，不能伪装成确定的未覆盖结果。
   - **覆盖统计**：忽略注释、空行和完全重复项，分别统计两端有效规则数；不要因某条规则被另一条包含就从总数中删除。源文件覆盖规则数是经语义判定已被目标完整覆盖的源规则数，全部判定完成时，未覆盖规则数等于源文件规则数减去覆盖规则数。局部覆盖的源规则按原规则列入未覆盖，不擅自生成拆分后的规则。
   - **输出表格**：对比完成后仅输出 Markdown 表格，固定包含“源文件规则数”“目标文件规则数”“源文件覆盖规则数”“未覆盖规则数”四列，不附解释段落，也不输出目标文件独有的规则。
   - **未覆盖明细**：以完成语义判断后，源文件中未被目标完整覆盖的规则数量判断；已被更宽泛规则覆盖的源规则不得列入。未覆盖 1–49 条时，在表格下方追加一个 `text` 代码块，按源文件顺序完整列出这些规则；未覆盖为 0 或大于等于 50 条时只输出表格，不添加代码块或省略列表。
   - **操作边界**：仅比较并输出，不修改规则、配置或 README；只有另行要求合并、删除或替换时才执行修改。
   - **示例**：“AliPay.list 对比 China_All.list”默认用 `rule/Surge/AliPay/AliPay.list` 对比 `Anm/Surge/rule/Inner/China/China_All.list`。

# 所有软件同步维护

1. 每次新增、更新、合并、去重、删除、迁移规则或调整规则配置，都必须检查并同步所有已接入软件的对应规则、必要的配置引用和 README。
2. 同步以分流意图和有效覆盖范围为准，不要求不同软件的文件逐字相同。优先采用上游对应软件的来源；缺少对应来源时，可从本次指定来源转换，必须记录转换方法。
3. 保留域名匹配方式、IP 网段、DNS 解析行为、策略与匹配顺序等语义，不得为凑齐数量机械复制不兼容语法。
4. 某软件不支持某类规则时，先检查能否等价转换；不能等价转换时，保留其已有有效行为，在 README 对应操作条目末尾简注差异，在交付说明中解释影响与原因。不得静默省略，或把部分同步报告为全部完成。
5. 已检查且无需修改规则的软件，保留其 README 原有条目即可，不追加检查记录；检查结论在交付说明中说明。

# 规则整理与注释

1. 剔除来源中的原有注释、标题和统计信息；只保留有效规则，并添加下述自定义注释。清理行内注释时不得截断规则值。
2. 每个来源或用途独立的规则组前，必须按顺序写出以下三个字段。支持 `#` 注释的软件使用：

   ```text
   # SOURCE: rule/Surge/China/China_All.list
   # UPDATE: yyyy-MM-dd HH:mm:ss
   # REMARK: 国内网站及相关服务的域名、IP 网段等规则，用于直连分流。
   ```

3. `SOURCE` 必须是可定位的仓库相对路径或完整来源 URL；多个来源分别成组，不得只写“来自网络”“国内规则”等模糊描述。手动补充规则须标明“用户自定义”，并指向对应 README 中保存具体规则的操作条目。
4. `UPDATE` 使用实际整理时间，时区统一为 `Asia/Shanghai`；同一批次的变更使用同一时间。只检查、未变更的规则组不刷新时间。
5. `REMARK` 用中文解释本组规则的大致用途；有转换或语义差异时简要说明。自定义规则文件只保留这三类维护注释，不保留源文件的 `NAME`、`AUTHOR`、`REPO`、`TOTAL` 等头部。
6. 新软件不支持 `#` 时，使用其合法注释方式保留三个字段；格式完全不支持注释时，在规则文件旁的 `<规则文件名>.meta.md` 中保存三个字段，不向规则文件写入非法内容；README 只在操作条目中简注这一差异。
7. 合并时默认只移除语义完全相同的重复项，保留规则顺序与来源归属。同一规则属于多个来源时，保留首次出现的规则，并在 README 用去重合并条目保留各来源关系，便于上游删除某一来源后重新判断。
8. 不得仅凭域名后缀或网段包含关系删除看似被覆盖的规则，也不得擅自改变策略、DNS 解析选项或匹配优先级。确需调整时，记录依据与影响。
9. 源内容、链接无法读取或来源不明确时，不得编造规则；保留现有有效内容，记录尚未完成的部分。

# 配置修改范围

| 软件 | 配置文件 | 默认允许修改的模块 |
| --- | --- | --- |
| Surge | `Anm/Surge/Surge.conf` | `[Rule]` |
| Loon | `Anm/Loon/Loon.lcf` | `[Remote Rule]` |

1. 未经用户明确授权，不得修改表中配置文件的其他模块。新软件的配置修改范围必须先由用户明确，或在适用的维护规范中明确；不明确时先完成规则整理并指出配置待确认项。
2. 配置中的规则链接前只写简洁的中文用途说明，例如 `# > 国内网站及相关服务直连`，不添加 `SOURCE`、`UPDATE`、`REMARK` 元信息。
3. 检查策略、引用路径、启用状态与顺序，避免重复引用；删除或迁移规则时同步处理引用。新增引用须位于适当位置，避免被前面的宽泛规则或最终规则遮蔽。
4. 沿用已确认的订阅地址约定，不得根据本地文件路径猜测远程发布地址。若订阅仓库与当前 fork 不同，在交付说明中说明路径映射及发布前提，不写入 README。

# 每个软件的 README 操作记录

README 仅按规则模块分组，每个模块下使用简洁的无序列表记录操作。每条操作默认一句话，写清来源、动作和目标；纯复制严格使用下面的句式即可。

```markdown
## ChinaMedia

- 将 `rule/Surge/ChinaMedia/ChinaMedia.list` 内容复制到 `Anm/Surge/rule/Inner/ChinaMedia/ChinaMedia.list`。
```

操作模板：

- 将 `xxx文件` 内容复制到 `xxx文件`。
- 将 `xxx文件` 内容合并到 `xxx文件`。
- 将 `xxx链接` 内容复制到 `xxx文件`。
- 将 `xxx链接` 内容合并到 `xxx文件`。
- 将 `xxx来源` 按 `必要的转换方法` 转换后写入 `xxx文件`。
- 向 `xxx文件` 添加、删除或替换 `具体规则`。

记录要求：

1. 只保留模块标题、操作条目及其必要的变更代码块，不添加维护范围、重建步骤章节、操作历史、日期、提交号、数量统计、验证报告、配置引用、发布说明或重复的三项注释。
2. 同步维护所有已接入软件的对应条目，反映当前有效操作；同一操作重复执行不重复追加，来源或操作方式变化时直接更新原条目。无需变化时不改 README。
3. 剔除源注释、添加三项注释、常规去重等通用要求由本规范统一约定，不在每条记录中重复说明。
4. 是否需要代码块取决于实际操作范围，不取决于是否使用“合并”“去重”等词：
   - **整份导入**：将整个文件或链接的有效规则复制、合并到本地文件，只记录一句“将 `xxx` 内容合并到 `xxx`。”（复制可写“内容复制到”），不附代码块。剔除源注释、格式转换和常规去重不改变整份导入的性质。
   - **局部变更**：对比内容后，仅选取部分规则合并或添加，或删除、替换本地规则，必须在操作条目下紧接代码块，记录本地规则实际变动的内容，不展开整个来源或未变化的规则。
   - 局部合并或添加使用 `text` 代码块列出实际新增规则；删除列出实际删除规则；替换或同时涉及增删时使用 `diff` 代码块，以 `-` 标记删除的旧规则、`+` 标记新增的规则。内容须完整，不包含未写入的重复项，不使用省略号。
   - 代码块与操作条目之间按 Markdown 格式留一个空行，不插入其他说明。没有实际变更时不追加记录，也不编造变更内容。
5. 必要的格式转换或软件差异在该条目中用一句短句或括号说明，不另写分析章节。链接来源使用完整 URL，文件使用仓库相对路径。

# 上游更新后的维护流程

1. 读取本规范及所有已接入软件的 README，确认当前有效来源、转换、合并、手动补充与排除项。
2. 检查工作区已有修改；以用户已更新的本地 fork 为基础读取最新来源，不改动上游目录。
3. 按 README 各模块的操作条目重新整理各来源，不在旧自定义结果上盲目追加。保留仍然有效的其他来源与用户补充，避免重新引入上游已删除的规则。
4. 对比更新前后，确认新增、删除、去重、格式转换及分流语义变化；来源消失或转换不再成立时，保留现有有效内容并报告具体问题。
5. 同步所有软件的规则、必要的配置引用和 README；存在软件差异时逐项说明。
6. 完成下述检查后，简要报告改动文件、规则变化、同步结果及待处理项。

# 完成前检查

- 本次改动仅在授权范围内，未触碰上游内容或用户已有修改。
- 所有已接入软件均已处理，各自 README 的操作条目与实际状态一致；未完成项在交付说明中明确。
- 来源规则无遗漏或意外增项，重复项处理符合记录；不同软件的差异有依据，不能只凭数量相同判定一致。
- 每组规则具有合法的 `SOURCE`、`UPDATE`、`REMARK`，没有残留的源注释。
- 配置仅改动允许的模块，链接说明简洁，路径、策略和顺序与记录一致。
- README 只包含模块标题、简洁操作条目及必要的变更代码块；整份导入只写一句，局部合并、添加、删除或替换附本地实际变更代码块，来源与目标明确，不添加维护范围、操作历史或验证报告。
- 检查差异和空白格式；有可用校验工具时验证目标软件语法。未运行客户端验证、未发布远程文件时，如实说明，不宣称已经生效。
